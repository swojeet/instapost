<!DOCTYPE html>
<html>
<head>
  <link href="https://fonts.googleapis.com/css?family=Satisfy" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Indie+Flower" rel="stylesheet">
   <!-- <link rel="shortcut icon" href="../../assets/favicon.ico" />  -->
  <%= favicon_link_tag 'favicon.ico' %>
  <title>Share Your Photos Online - Instapost</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav>
    <div class="row" style="padding: 0px 50px;">
       <div class="nav_wrapper">
            <div class="col-md-4">
                <span class="pull-left brand"><i class="fa fa-instagram" aria-hidden="true"></i>  | <%= link_to 'Instapost', root_path %></span>
            </div>
            <div class="col-md-4">
				<div class="row">
					<div class="input-group" style="margin-top: 22px; width: 280px;">
 						  <span class="input-group-addon" id="basic-addon1"><i class="fa fa-search" aria-hidden="true"></i></span>
 						  <input type="search" class="form-control" placeholder="Search" aria-describedby="basic-addon1" > 
					</div>
				</div>
            </div>
            <div class="col-md-4">
                <% if user_signed_in? %>
                    <span class="nav_links pull-right"><%= link_to 'New Post', new_post_path %></span>
                    <span class="nav_links pull-right"><%= link_to 'Edit Settings', edit_user_registration_path %></span>
                    <span class="nav_links pull-right"><%= link_to 'Log out', destroy_user_session_path, method: :delete %></span>
                <% else %>
                    <%= link_to 'Sign up', new_user_registration_path, class: 'nav_links pull-right' %>
                    <%= link_to 'Log in', new_user_session_path, class: 'nav_links pull-right' %>
                <% end %>
            </div>
        </div>
    </div>
</nav>
<%= yield %>
</body>
</html>